# -----------------------------
# InferenceService (Gemma 4 27B MoE)
# -----------------------------
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: gemma4-moe
spec:
  predictor:
    deploymentStrategy:
      type: Recreate
    runtimeClassName: nvidia
    nodeSelector:
      feature.node.kubernetes.io/pci-10de.present: "true"
    tolerations:
      - key: "nvidia.com/gpu"
        operator: "Exists"
        effect: "NoSchedule"
    model:
      modelFormat:
        name: huggingface
      runtime: vllm-gemma4-runtime
      env:
        - name: MODEL_ID
          value: "cyankiwi/gemma-4-26B-A4B-it-AWQ-4bit"
        - name: PROMPT_TOKEN_PRICE
          value: "0.6953"
        - name: PROMPT_CACHED_TOKEN_PRICE
          value: "0.0695"
        - name: COMPLETION_TOKEN_PRICE
          value: "3.6859"
        - name: COMPLETION_CACHED_TOKEN_PRICE
          value: "0.3686"
      resources:
        limits:
          nvidia.com/gpu: 1
          memory: "12Gi"
        requests:
          nvidia.com/gpu: 1
          memory: "12Gi"
